import 'package:flutter/widgets.dart';

class StoriesBarSliver extends StatelessWidget {
  const StoriesBarSliver({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
